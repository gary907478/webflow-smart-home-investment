/* eslint-disable no-console */

/**
 * Greets the user by printing a message in the console.
 * @param version current version.
 */
export const showVersion = (version: string) => {
  console.log(`Now Version is ${version}!`);
};
